<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="com.example.gestionequipos.ui.home.HomeFragment"
        android:label="@string/menu_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_nav_home_to_nav_partediario"
            app:destination="@id/nav_partediario" />
    </fragment>
    <fragment
        android:id="@+id/nav_partediario"
        android:name="com.example.gestionequipos.ui.partediario.ParteDiarioFragment"
        android:label="@string/menu_partediario" />
</navigation>